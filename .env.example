# Flask
FLASK_ENV=development
PORT=5000
CORS_ORIGINS=*

# Database (optional — falls back to SQLite if empty)
MONGODB_URI=
DATABASE_URL=

# IBM Watson Machine Learning (optional — app works without this)
# WATSON_API_KEY=
# WATSON_URL=
# WATSON_DEPLOYMENT_ID=
